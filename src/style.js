const styles = {
    flexSpace: "flex items-center justify-between",

    flexCenter: "flex items-center justify-center",
    
    flexStart: "flex items-center justify-start",
}

export const layout = {

    arrow : `cursor-pointer bg-transparent outline-none text-red-600 ${flexCenter} text-lg rounded-md absolute bottom-md right-4 scale-100 transition-all duration-none hover:scale-130`
}
export default styles